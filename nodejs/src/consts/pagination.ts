export const LIMIT_PATTERN = '^($|100|[1-9][0-9]?)$' // пустая строка либо от 1 до 100
export const PAGE_PATTERN = '^$|^[1-9]\\d*$' // любое число от 1 или пустая строка
export const DEFAULT_PAGE = 1
export const DEFAULT_LIMIT = 20
